import React from "react";

const Loading = () => {
  return (
    <div className="flex-1 w-full h-screen flex items-center justify-center">
      <div className="loader m-auto my-4" />
    </div>
  );
};

export default Loading;
